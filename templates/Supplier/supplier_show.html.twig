{% extends '@SonataAdmin/CRUD/show.html.twig' %}

{% block actions %}
    <ul class="dropdown-menu show" role="menu">
        {% if object is not null and admin.id(object) is not null and admin.hasAccess('edit', object) and admin.hasRoute('edit') %}
            <li>
                <a href="{{ path('admin_app_supplier_edit', {'id': object.id}) }}" class="btn btn-primary sonata-action-element" title="{{ 'action_edit'|trans({},'SonataAdminBundle') }}">
                    <i class="fas fa-pencil-alt" aria-hidden="true"></i>
                    {{ 'action_edit'|trans({}, 'SonataAdminBundle') }}
                </a>
            </li>
        {% endif %}
        <li>
            <a class="btn btn-default sonata-action-element" href="{{ path('admin_app_supplier_list') }}">
                <i class="fas fa-list" aria-hidden="true"></i>
                Return to list
            </a>
        </li>
    </ul>
{% endblock %}